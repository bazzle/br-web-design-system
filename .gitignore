# Dependencies
node_modules/

# Generated CSS — rebuilt on publish, never committed (source of truth is tokens/)
dist/css/
# NOTE: dist/figma/ is intentionally committed so token diffs are reviewable in PRs.

# Build / Storybook output
storybook-static/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
